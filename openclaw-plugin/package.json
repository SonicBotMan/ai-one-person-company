{
  "name": "@soloflow/openclaw-plugin",
  "version": "0.1.0",
  "description": "TypeScript plugin for OpenClaw — AI Agent framework integration for SoloFlow",
  "type": "module",
  "openclaw": {
    "extensions": [
      "./dist-esm/index.js"
    ]
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "build:bundle": "npx esbuild src/index.ts --bundle --platform=node --format=esm --outfile=dist-esm/index.js --external:better-sqlite3 --external:openclaw --external:@sinclair/typebox",
    "dev": "tsc --watch",
    "test": "echo 'no tests yet'",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "openclaw",
    "soloflow",
    "ai-agent",
    "plugin",
    "mcp",
    "llm"
  ],
  "author": "SonicBotMan",
  "license": "MIT",
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "@sinclair/typebox": "^0.34.49",
    "@types/better-sqlite3": "^7.6.13",
    "openclaw": "^2026.4.23",
    "typescript": "^5.9.3"
  },
  "peerDependencies": {
    "typescript": ">=5.0.0"
  },
  "dependencies": {
    "better-sqlite3": "^12.8.0",
    "jose": "^6.2.2",
    "yaml": "^2.8.3"
  }
}
